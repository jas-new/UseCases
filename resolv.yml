---
- name: check DNS entries
  hosts: localhost
  tasks:
     - name: check all the dns entries
       shell: cat /etc/resolv.conf >> /tmp/dns1.txt
       
     - name: fetch nginx access log
       fetch:
          src: /tmp/dns1.txt
          dest: /tmp
          flat: yes
       delegate_to: localhost

